# SwiftUI App - No CocoaPods dependencies needed
# Add any native iOS dependencies here if needed in the future

platform :ios, '15.0'

target 'TradeLeague' do
  use_frameworks!
  
  # Add Swift Package Manager dependencies through Xcode
  # or add CocoaPods here if needed for native iOS libraries
end